# Python - Classes and Objects

This project introduces Object-Oriented Programming in Python: classes,
objects, instances, attributes (public, protected, private), the
special `__init__` method, properties as Pythonic getters/setters, and
how Python resolves attributes through an instance or class `__dict__`.
