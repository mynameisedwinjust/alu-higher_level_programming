# Python - Everything is Object

This project explores CPython's object model: id() and type(), the
difference between mutable and immutable objects, the distinction
between assignment and referencing (aliasing), how arguments are
passed to functions, and small integer/string caching behavior.
